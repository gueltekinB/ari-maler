# Lokales CSS-Build-Tooling (auf dem Server nicht benötigt)
/node_modules

# misc
.DS_Store
*.pem

# debug
npm-debug.log*

# env files (falls je benötigt)
.env*

# Geheime Schlüssel (nur lokal und per FTP auf dem Server)
includes/secrets.php
